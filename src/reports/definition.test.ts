import { expect, it } from "vitest";
import { reportDefinition, reportWidgetSites, reportDefinitionFromSearch } from "./definition";
it("preserves widget ordering and independent website and date settings",()=>{const result=reportDefinition("tpl-domain",{days:28,sections:["Search trend","Executive summary"],widgets:{"Search trend":{site:"globalbusrental",days:7}}});expect(result.sections).toEqual(["Search trend","Executive summary"]);expect(result.widgets?.["Search trend"]).toEqual({site:"globalbusrental",days:7});expect(reportWidgetSites(result)).toEqual(["globalbusrental"]);});
it("rejects unsupported periods and unknown or malformed widgets",()=>{for(const input of [{days:14},{widgets:{"Search trend":{days:365}}},{widgets:{unknown:{days:7}}},{widgets:{"Search trend":{site:"../../hidden"}}},{widgets:[]}])expect(()=>reportDefinition("tpl-domain",input)).toThrow();});

it("opens the exact template, section order and widget settings carried by a report link",()=>{const result=reportDefinitionFromSearch(new URLSearchParams({template:"tpl-domain",days:"90",widgets:JSON.stringify({"Search trend":{days:7}}),section:"Search trend"}));expect(result.days).toBe(90);expect(result.sections).toEqual(["Search trend"]);expect(result.widgets?.["Search trend"]?.days).toBe(7);expect(()=>reportDefinitionFromSearch(new URLSearchParams({widgets:"{bad"}))).toThrow("Invalid report widget");});
